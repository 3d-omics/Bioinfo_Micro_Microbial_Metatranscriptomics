#!/usr/bin/env bash

set -euo pipefail

snakemake \
    --use-conda \
    --rerun-incomplete \
    --cache \
    --jobs 100 \
    --cores 24  \
    --keep-going \
    --profile profile/default \
    --latency-wait 60 \
    "$@"
